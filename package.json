{
  "name": "webcam-biometrics-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "@types/node": "^25.0.10"
  }
}
